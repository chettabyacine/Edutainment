import 'package:flutter/material.dart';
class QuestionAnimalsOrGeometry{
  bool usesInput;//weather or not the user inputs the answer as a string
  Widget questionWidget;
  QuestionAnimalsOrGeometry({this.usesInput,this.questionWidget});
}